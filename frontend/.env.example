# For local development (running frontend with npm run dev):
NEXT_PUBLIC_API_URL=http://localhost:8000

# When using docker-compose, this is overridden to http://backend:8000
# No need to change this file when using docker-compose
